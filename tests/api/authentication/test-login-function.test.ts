/**
 * Test file để chạy thử hàm loginAndSaveToken
 */

const { accounts } = require('../../config/api/accounts.config');
const { login } = require('../../services/api/authenticationApi/login.api');

describe("Test loginAndSaveToken function", () => {
  test("TC001_Should login and save token successfully", async () => {
  });
});